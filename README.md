# 🚀 ModernVPN: The Ultimate Multi-Protocol Tunneling Solution

تطبيق أندرويد متكامل يجمع بين القوة والمرونة، مصمم لتوفير اتصال آمن وتخطي الرقابة الجغرافية باستخدام أحدث تقنيات الأنفاق (Tunneling). يتميز المشروع ببنية برمجية حديثة تضمن استقرار الاتصال وسرعة الأداء.

---

## 🛠 المحركات والبروتوكولات المدعومة (Core Engines)

يتميز **ModernVPN** بقدرته على إدارة وتشغيل مجموعة واسعة من البروتوكولات العالمية في تطبيق واحد:

* **Xray (V2Ray):** دعم كامل لبروتوكولات VLESS و VMess مع إدارة ذكية للمسارات عبر ملفات `geoip` و `geosite`.
* **SSH Tunneling:** توفير نفق آمن باستخدام مكتبة `Trilead SSH2` لدعم اتصالات SSH Direct و HTTP Proxy.
* **OpenVPN:** دمج كامل لمحرك OpenVPN الرسمي لاتصالات مشفرة بمعايير عالمية.
* **Psiphon:** تقنيات مقاومة الحجب المتقدمة لضمان الوصول الدائم للإنترنت في أصعب الظروف.
* **DNSTT (DNS Tunneling):** القدرة على نقل البيانات عبر طلبات DNS لتخطي جدران الحماية الصارمة.
* **UDP Hysteria & UdpV2:** تحسين زمن الاستجابة (Ping) للألعاب والبث المباشر عبر بروتوكولات UDP سريعة.

---

## ✨ المميزات التقنية المتقدمة

* **تشفير بمعايير عسكرية:** دمج مكتبات `Google Tink` و `EdDSA` لتوفير طبقات حماية إضافية للبيانات.
* **نظام العمليات المنفصلة:** يعمل محرك Xray في عملية مستقلة (`:xray_core_process`) لضمان عدم تأثر التطبيق الرئيسي بأي ضغط على المعالج.
* **أدوات المطورين المدمجة:** يحتوي التطبيق على أدوات فحص متقدمة مثل `Host Checker`, `Port Scanner`, `IP Hunter`, و `Ping Monitor`.
* **استيراد ذكي وتلقائي:** دعم جلب الإعدادات عبر مسح كود QR، الروابط المباشرة (URL)، أو الملفات المشفرة `.mvpn`.
* **حماية الخصوصية:** ميزات مدمجة لمنع تسريب DNS، دعم الـ Kill Switch، وتخطي تطبيقات محددة عبر الـ Split Tunneling.

---

## 🌐 روابط النظام المتكامل (Test Environment)

| العنصر | الرابط التجريبي | الوصف |
| :--- | :--- | :--- |
| **صفحة الهبوط** | `www.modern-vpn-test.com` | واجهة الموقع الرسمية لتحميل التطبيق |
| **سكربت VPS** | `api.modern-vpn-test.com/install` | السكربت السريع لتهيئة السيرفرات وإدارة الحسابات |
| **دليل المزايا** | `docs.modern-vpn-test.com` | شرح مفصل لكل ميزة داخل التطبيق |

---

## 🚀 كيفية التشغيل (للمطورين)

1. قم بفتح المشروع باستخدام **Android Studio** (نسخة Ladybug أو أحدث).
2. تأكد من أن الـ `compileSdk` مضبوط على **36** و الـ `minSdk` على **24**.
3. المشروع يعتمد على **View Binding** و **KSP** لمعالجة البيانات بكفاءة عالية.
4. يتم نسخ ملفات المحرك الأساسية (`geoip.dat`, `geosite.dat`) تلقائياً عند أول تشغيل لضمان جاهزية المحرك.

---

### 💡 ملاحظة المطور
تم بناء هذا المشروع بشغف ليكون منافساً قوياً في سوق تطبيقات الـ VPN، مع التركيز على جعل الواجهة بسيطة للمستخدم العادي والقدرات عميقة للمستخدم المحترف.
